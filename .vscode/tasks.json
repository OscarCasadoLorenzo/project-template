{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "Install Dependencies",
      "type": "shell",
      "command": "npm install && cd backend && npm install && cd ../renderer && npm install",
      "group": "build",
      "problemMatcher": []
    }
  ]
}
